name: Build and Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-ubuntu:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      name: Checkout Code
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: |
          5.0.x
          6.0.x
    - name: Build (Framework 2.0)
      run: msbuild /t:restore ./src/net20/src.net20.csproj
    - name: Build (Framework 4.0)
      run: msbuild /t:restore ./src/net40/src.net40.csproj
    - name: Build (Framework 4.5)
      run: msbuild /t:restore ./src/net45/src.net45.csproj
    - name: Build (DotNet Core 5.0 and NetStandard 2.0)
      run: dotnet build ./csharp-csv-reader.sln
    - name: Test (net60)
      run: dotnet test ./tests/net60/tests.net60.csproj
    - name: Test (net50)
      run: dotnet test ./tests/net50/tests.net50.csproj
    - name: Install NUnit.ConsoleRunner 3.4.0 (compatibility)
      run: nuget install NUnit.ConsoleRunner -Version 3.4.0 -DirectDownload -OutputDirectory .
    - name: Test (net45)
      run: ./NUnit.ConsoleRunner.3.4.0/tools/nunit3-console.exe ./tests/net45/tests.net45.csproj
    - name: Test (net40)
      run: ./NUnit.ConsoleRunner.3.4.0/tools/nunit3-console.exe ./tests/net40/tests.net40.csproj
    - name: Test (net20)
      run: ./NUnit.ConsoleRunner.3.4.0/tools/nunit3-console.exe ./tests/net20/tests.net20.csproj
